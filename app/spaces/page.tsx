import type { Metadata } from 'next'; import { spaces } from '@/data/content'; import Image from 'next/image'; import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react';
export const metadata: Metadata = { title:'Spaces | Al Wahid Furnitures', description:'Furniture for every space — home, office, corporate, school, institutional and custom.' };
export default function SpacesPage(){return <div className="pt-[82px]"><section className="section-pad"><div className="container-wide"><p className="eyebrow">Spaces</p><h1 className="mt-4 max-w-2xl font-display text-6xl leading-[.92] tracking-[-.03em] text-[var(--green)] sm:text-7xl">Furniture for Every Space.</h1><p className="mt-6 max-w-md text-sm leading-7 text-[var(--charcoal)]/65">Each space has its own rhythm. Browse furniture developed around how each one works.</p><div className="mt-16 grid gap-4 md:grid-cols-2">{spaces.map(s=>(<Link key={s.slug} href={`/spaces/${s.slug}`} className="group relative overflow-hidden"><div className="relative aspect-[1/1.1]"><Image src={s.image} alt={s.name} fill sizes="(max-width: 767px) 100vw, 50vw" className="object-cover transition duration-700 group-hover:scale-105"/></div><div className="absolute inset-0 flex flex-col justify-end bg-gradient-to-t from-[rgba(17,21,18,.7)] to-transparent p-6"><p className="eyebrow text-[var(--ivory)]/70">{s.eyebrow}</p><h2 className="mt-2 font-display text-4xl text-[var(--ivory)]">{s.name}</h2><p className="mt-2 max-w-xs text-sm text-[var(--ivory)]/80">{s.description}</p><span className="mt-3 inline-flex items-center gap-1 text-xs font-bold uppercase tracking-[.12em] text-[var(--ivory)]">Explore <ArrowUpRight size={14} className="transition-transform group-hover:translate-x-1"/></span></div></Link>))}</div></div></section></div>}
