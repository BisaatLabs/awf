import { notFound } from 'next/navigation'; import type { Metadata } from 'next'; import Image from 'next/image'; import Link from 'next/link'; import { ArrowRight } from 'lucide-react'; import { projects } from '@/data/content'; import { corporateWhatsAppUrl } from '@/lib/whatsapp';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=projects.find(p=>p.slug===slug);if(!p)return{title:'Project not found | Al Wahid Furnitures'};return{title:`${p.title} | Al Wahid Furnitures`,description:p.overview}}
export default async function ProjectDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const project=projects.find(p=>p.slug===slug);if(!project)notFound();return <div className="pt-[82px]"><section className="relative h-[60vh] min-h-[420px] overflow-hidden"><Image src={project.image} alt={project.title} fill priority className="object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-[rgba(17,21,18,.6)] to-transparent"/><div className="container-wide relative z-10 flex h-full flex-col justify-end pb-12 text-[var(--ivory)]"><p className="eyebrow text-[var(--ivory)]/70">{project.type}</p><h1 className="mt-4 font-display text-6xl leading-[.92] tracking-[-.03em] sm:text-7xl">{project.title}</h1></div></section><section className="section-pad"><div className="container-wide grid gap-12 lg:grid-cols-[1fr_2fr] lg:gap-16"><div><p className="eyebrow">Overview</p><p className="mt-4 text-base leading-7 text-[var(--charcoal)]/70">{project.overview}</p><div className="mt-8 grid gap-px bg-[var(--line)]"><div className="bg-[var(--ivory)] p-5"><p className="text-[10px] font-bold uppercase tracking-[.13em] text-[var(--walnut)]">Scope</p><p className="mt-2 text-sm text-[var(--charcoal)]">{project.scope.join(', ')}</p></div><div className="bg-[var(--ivory)] p-5"><p className="text-[10px] font-bold uppercase tracking-[.13em] text-[var(--walnut)]">Materials</p><p className="mt-2 text-sm text-[var(--charcoal)]">{project.materials.join(', ')}</p></div></div></div><div className="grid gap-4">{project.gallery.map((img,i)=>(<div key={i} className="relative aspect-[4/3] overflow-hidden"><Image src={img} alt={`${project.title} — image ${i+1}`} fill sizes="(max-width: 1023px) 100vw, 66vw" className="object-cover"/></div>))}</div></div></section><section className="bg-[var(--green)] py-24"><div className="container-wide flex flex-col items-center gap-6 text-center text-[var(--ivory)]"><h2 className="max-w-2xl font-display text-4xl leading-[.95] sm:text-5xl">Have a Similar Project?</h2><p className="max-w-md text-sm text-[var(--ivory)]/80">Request a project consultation and we’ll help you plan the right furniture package.</p><div className="flex flex-wrap justify-center gap-4"><Link href="/contact" className="inline-flex items-center gap-2 bg-[var(--ivory)] px-6 py-4 text-xs font-bold uppercase tracking-[.13em] text-[var(--green)] transition hover:bg-[var(--walnut)] hover:text-[var(--ivory)]">Request a Project Consultation <ArrowRight size={15}/></Link><a href={corporateWhatsAppUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 border border-[var(--ivory)]/40 px-6 py-4 text-xs font-bold uppercase tracking-[.13em] text-[var(--ivory)] transition hover:bg-[var(--ivory)] hover:text-[var(--green)]">Discuss a Bulk Order</a></div></div></section></div>}
