import type { Metadata } from 'next'; import { projects } from '@/data/content'; import Image from 'next/image'; import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react';
export const metadata: Metadata = { title:'Projects | Al Wahid Furnitures', description:'Sample project case studies showing how Al Wahid Furnitures develops furniture for corporate, office, school, institutional and residential spaces.' };
const types=['Corporate','Office','School','Institutional','Residential'];
export default function ProjectsPage(){return <div className="pt-[82px]"><section className="section-pad"><div className="container-wide"><p className="eyebrow">Projects</p><h1 className="mt-4 max-w-2xl font-display text-6xl leading-[.92] tracking-[-.03em] text-[var(--green)] sm:text-7xl">Spaces Built Around People.</h1><p className="mt-6 max-w-md text-sm leading-7 text-[var(--charcoal)]/65">Explore illustrative furniture concepts for homes, workplaces, and institutions. These sample projects show possible applications and are not completed client case studies.</p><div className="mt-8 flex flex-wrap gap-3">{types.map(t=><span key={t} className="border border-[var(--line)] px-4 py-2 text-xs font-bold uppercase tracking-[.12em] text-[var(--charcoal)]/60">{t}</span>)}</div><div className="mt-16 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{projects.map(p=>(<Link key={p.slug} href={`/projects/${p.slug}`} className="group"><div className="relative aspect-[4/3] overflow-hidden"><Image src={p.image} alt={p.title} fill sizes="(max-width: 767px) 100vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105"/></div><div className="border-b border-[var(--line)] py-5"><p className="text-[10px] font-bold uppercase tracking-[.15em] text-[var(--walnut)]">{p.type}</p><h2 className="mt-2 font-display text-3xl text-[var(--charcoal)]">{p.title}</h2><p className="mt-2 text-sm leading-6 text-[var(--charcoal)]/55">{p.overview}</p><span className="mt-3 inline-flex items-center gap-1 text-xs font-bold uppercase tracking-[.12em] text-[var(--green)]">View Project <ArrowUpRight size={14} className="transition-transform group-hover:translate-x-1"/></span></div></Link>))}</div></div></section></div>}
