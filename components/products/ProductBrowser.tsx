'use client';
import { useMemo, useState } from 'react'; import { Search, SlidersHorizontal } from 'lucide-react'; import type { Product } from '@/data/content'; import { ProductCard } from './ProductCard';
const categories=['All','Tables','Desks','Chairs','Sofas','Beds','Storage','Office','Dining','Custom']; const spaces=['All','Home','Office','Corporate','Institutional','School']; const materials=['All','Wood','Laminate','Metal','Upholstery','Mixed Materials'];
export function ProductBrowser({products}:{products:Product[]}){const [query,setQuery]=useState('');const [category,setCategory]=useState('All');const [space,setSpace]=useState('All');const [material,setMaterial]=useState('All');const filtered=useMemo(()=>products.filter(p=>(!query||`${p.name} ${p.category} ${p.materials.join(' ')}`.toLowerCase().includes(query.toLowerCase()))&&(category==='All'||p.category===category)&&(space==='All'||p.space===space)&&(material==='All'||p.materials.includes(material))),[products,query,category,space,material]);return <div><div className="mb-10 border-y border-[var(--line)] py-5"><div className="flex flex-col gap-5 lg:flex-row lg:items-center lg:justify-between"><label className="flex flex-1 items-center gap-3 border-b border-[var(--line)] pb-3 lg:max-w-sm"><Search size={17} className="text-[var(--walnut)]"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search the collection" className="w-full bg-transparent text-sm outline-none placeholder:text-[var(--charcoal)]/40"/></label><div className="flex items-center gap-2 overflow-x-auto pb-1"><SlidersHorizontal size={16} className="mr-1 shrink-0 text-[var(--walnut)]"/>{categories.map(item=><button key={item} onClick={()=>setCategory(item)} className={`whitespace-nowrap border px-3 py-2 text-[10px] font-bold uppercase tracking-[.1em] transition ${category===item?'border-[var(--green)] bg-[var(--green)] text-[var(--ivory)]':'border-[var(--line)] text-[var(--charcoal)]/60 hover:border-[var(--green)]'}`}>{item}</button>)}</div></div><div className="mt-5 flex flex-wrap gap-2">{spaces.map(item=><button key={item} onClick={()=>setSpace(item)} className={`text-xs ${space===item?'font-bold text-[var(--green)]':'text-[var(--charcoal)]/45'}`}>{item}{item!=='All'&&' /'}</button>)}<span className="mx-2 text-[var(--stone)]">|</span>{materials.map(item=><button key={item} onClick={()=>setMaterial(item)} className={`text-xs ${material===item?'font-bold text-[var(--walnut)]':'text-[var(--charcoal)]/45'}`}>{item}{item!=='All'&&' /'}</button>)}</div></div><p className="mb-6 text-xs uppercase tracking-[.12em] text-[var(--charcoal)]/45">Showing {filtered.length} pieces</p>{filtered.length?<div className="grid gap-x-6 gap-y-12 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="border border-[var(--line)] py-24 text-center"><p className="font-display text-3xl">No pieces found.</p><button onClick={()=>{setQuery('');setCategory('All');setSpace('All');setMaterial('All')}} className="mt-4 text-xs font-bold uppercase tracking-[.12em] text-[var(--green)]">Reset filters</button></div>}</div>}
