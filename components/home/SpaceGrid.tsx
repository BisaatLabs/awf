import Image from 'next/image'; import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import { spaces } from '@/data/content';
export function SpaceGrid(){return <section className="section-pad"><div className="container-wide"><div className="mb-12 flex flex-col justify-between gap-6 border-t border-[var(--line)] pt-5 md:flex-row md:items-end"><div><p className="eyebrow">02 / Shop by Space</p><h2 className="mt-4 max-w-2xl font-display text-5xl leading-[.95] tracking-[-.03em] text-[var(--green)] sm:text-6xl">Furniture for Every Space.</h2></div><Link href="/spaces" className="inline-flex shrink-0 items-center gap-2 border-b border-[var(--green)] pb-2 text-xs font-bold uppercase tracking-[.12em] text-[var(--green)]">View all spaces <ArrowUpRight size={15}/></Link></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{spaces.map((space,i)=>(<Link key={space.slug} href={`/spaces/${space.slug}`} className={`group relative overflow-hidden ${i===0?'lg:row-span-2':''}`}><div className={`relative ${i===0?'aspect-[1/1.4] lg:aspect-auto lg:h-full':'aspect-[1/1.05]'}`}><Image src={space.image} alt={space.name} fill sizes="(max-width: 767px) 100vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105"/></div><div className="absolute inset-0 flex flex-col justify-end bg-gradient-to-t from-[rgba(17,21,18,.7)] to-transparent p-6"><p className="eyebrow text-[var(--ivory)]/70">{space.eyebrow}</p><h3 className="mt-2 font-display text-4xl text-[var(--ivory)]">{space.name}</h3><p className="mt-2 max-w-xs text-sm leading-6 text-[var(--ivory)]/80">{space.description}</p><span className="mt-3 inline-flex items-center gap-1 text-xs font-bold uppercase tracking-[.12em] text-[var(--ivory)]">Explore <ArrowUpRight size={14} className="transition-transform group-hover:translate-x-1"/></span></div></Link>))}</div></div></section>}
