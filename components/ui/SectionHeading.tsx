import { ArrowUpRight } from 'lucide-react';
export function SectionHeading({eyebrow,title,copy,link}:{eyebrow:string;title:string;copy?:string;link?:{label:string;href:string}}){return <div className="mb-12 flex flex-col justify-between gap-6 border-t border-[var(--line)] pt-5 md:flex-row md:items-end"><div><p className="eyebrow">{eyebrow}</p><h2 className="mt-4 max-w-2xl font-display text-5xl leading-[.95] tracking-[-.03em] text-[var(--green)] sm:text-6xl">{title}</h2>{copy&&<p className="mt-5 max-w-xl text-sm leading-7 text-[var(--charcoal)]/65">{copy}</p>}</div>{link&&<a href={link.href} className="inline-flex shrink-0 items-center gap-2 border-b border-[var(--green)] pb-2 text-xs font-bold uppercase tracking-[.12em] text-[var(--green)]">{link.label}<ArrowUpRight size={15}/></a>}</div>}
